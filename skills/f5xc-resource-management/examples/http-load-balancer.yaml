metadata:
  name: example-app-lb
  namespace: demo-app
spec:
  domains:
    - my-app.example.com
  http:
    dns_volterra_managed: true
  default_route_pools:
    - pool:
        name: example-app-pool
        namespace: demo-app
      weight: 1
